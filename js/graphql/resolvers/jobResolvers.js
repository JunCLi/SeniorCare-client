import { GET_JOB_FORM } from '../queries/jobQueries'

// import gql from 

export default {
	Mutation: {

	},
	Query: {
		// async getJobForm(parent, input, { cache }) {
		// 	const readData = cache.readQuery({
		// 		query:
		// 	})
		// }
	},
}